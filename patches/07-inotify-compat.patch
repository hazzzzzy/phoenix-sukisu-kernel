--- a/fs/notify/fdinfo.c
+++ b/fs/notify/fdinfo.c
@@ -18,6 +18,11 @@
 #include <linux/susfs_def.h>
 #endif

+/* 4.14 不存在 inotify_mark_user_mask(), susfs patch 用了它; 这里 shim */
+#ifndef inotify_mark_user_mask
+#define inotify_mark_user_mask(m) ((m)->mask & IN_ALL_EVENTS)
+#endif
+
 #include "inotify/inotify.h"
 #include "../fs/mount.h"

