name: Build Phoenix SukiSU Kernel

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    name: Build kernel
    runs-on: ubuntu-22.04
    env:
      KERNEL_REPO: https://github.com/MiCode/Xiaomi_Kernel_OpenSource.git
      KERNEL_BRANCH: phoenix-r-oss
      DEFCONFIG: vendor/phoenix_user_defconfig
      # ShirkNeko 时代老 SukiSU（完美支持 4.x 非 GKI 内核）
      KSU_REPO: https://github.com/omeos-fork/ShirkNeko.SukiSU-Ultra.git
      KSU_BRANCH: susfs-stable
      ARCH: arm64
    steps:
      - name: Checkout workflow repo
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bc bison build-essential ccache curl flex \
            git libelf-dev libncurses5-dev libssl-dev \
            lld make rsync zip unzip cpio python3

      - name: Shallow clone phoenix-r-oss kernel
        run: |
          git clone --depth=1 -b "$KERNEL_BRANCH" "$KERNEL_REPO" kernel
          cd kernel
          echo "kernel HEAD: $(git rev-parse HEAD)"
          head -5 Makefile

      - name: Fetch Proton Clang toolchain
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git toolchain/clang

      - name: Fetch AOSP GCC (arm64 + arm)
        run: |
          git clone --depth=1 -b android-10.0.0_r47 \
            https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 \
            toolchain/gcc-arm64
          git clone --depth=1 -b android-10.0.0_r47 \
            https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 \
            toolchain/gcc-arm

      - name: Integrate ShirkNeko SukiSU (manual hook + 内建 susfs)
        working-directory: kernel
        run: |
          # 直接 git clone fork（原 ShirkNeko 仓库已 404，不能用 setup.sh）
          git clone --depth=1 -b "$KSU_BRANCH" "$KSU_REPO" KernelSU
          # drivers/kernelsu 软链到 KernelSU/kernel
          ln -sf "../KernelSU/kernel" "drivers/kernelsu"
          # 验证解析正确
          test -f drivers/kernelsu/Kconfig || (echo "FAIL: symlink target 不存在" && ls -la drivers/kernelsu && exit 1)
          # drivers/Makefile 挂载
          grep -q "obj-\$(CONFIG_KSU)" drivers/Makefile || \
            printf '\nobj-$(CONFIG_KSU) += kernelsu/\n' >> drivers/Makefile
          # drivers/Kconfig 注入
          grep -q 'source "drivers/kernelsu/Kconfig"' drivers/Kconfig || \
            sed -i '/^endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig
          echo "--- drivers/Makefile 末尾 ---"; tail -3 drivers/Makefile
          echo "--- drivers/kernelsu 目录 ---"; ls drivers/kernelsu/ | head -15

      - name: Apply susfs4ksu kernel-4.14 内核侧 patch
        working-directory: kernel
        run: |
          git clone --depth=1 -b kernel-4.14 https://gitlab.com/simonpunk/susfs4ksu.git ../susfs4ksu
          cp ../susfs4ksu/kernel_patches/fs/susfs.c    fs/
          cp ../susfs4ksu/kernel_patches/fs/sus_su.c   fs/
          cp ../susfs4ksu/kernel_patches/include/linux/susfs.h      include/linux/
          cp ../susfs4ksu/kernel_patches/include/linux/susfs_def.h  include/linux/
          cp ../susfs4ksu/kernel_patches/include/linux/sus_su.h     include/linux/
          patch -p1 -F 3 --no-backup-if-mismatch < ../susfs4ksu/kernel_patches/50_add_susfs_in_kernel-4.14.patch | tee /tmp/susfs_apply.log
          if grep -q "FAILED" /tmp/susfs_apply.log; then
            echo "==== susfs patch 有失败 ===="
            find . -name "*.rej" -exec sh -c 'echo "---$1---"; cat "$1"' _ {} \;
            exit 1
          fi

      - name: Fix phoenix 源码自身 Makefile 小瑕疵
        working-directory: kernel
        run: |
          # drivers/nfc/Makefile 末尾误加 "/" 导致 built-in.o 路径双斜杠
          sed -i 's| /$||' drivers/nfc/Makefile
          grep -n "nq-nci" drivers/nfc/Makefile || true

          # drivers/scsi/ufs/Makefile 把 ufshcd.o 加了两次 (once via ufshcd-core-y, once direct)
          # 删掉独立那行, 保留 ufshcd-core.o 即可
          sed -i '/^obj-\$(CONFIG_SCSI_UFSHCD) += ufshcd\.o$/d' drivers/scsi/ufs/Makefile
          grep "ufshcd" drivers/scsi/ufs/Makefile

      - name: Apply manual hook patches (6 files)
        working-directory: kernel
        run: |
          for p in ../patches/*.patch; do
            echo "=== apply $(basename $p) ==="
            patch -p1 -F 3 --no-backup-if-mismatch < "$p"
          done
          if find . -name "*.rej" | grep -q .; then
            echo "==== manual hook patch 失败 ===="
            find . -name "*.rej" -exec sh -c 'echo "---$1---"; cat "$1"' _ {} \;
            exit 1
          fi

      - name: Patch defconfig (KSU_MANUAL_HOOK + SUSFS 全家桶, 关 KPM)
        working-directory: kernel
        run: |
          D="arch/arm64/configs/${DEFCONFIG}"
          set_kv() {
            local key=$1 val=$2
            if grep -qE "^(# )?${key}[= ]" "$D"; then
              sed -i -E "s|^(# )?${key}.*$|${key}=${val}|" "$D"
            else
              echo "${key}=${val}" >> "$D"
            fi
          }
          unset_kv() {
            local key=$1
            sed -i -E "/^(# )?${key}[= ]/d" "$D"
            echo "# ${key} is not set" >> "$D"
          }

          # KSU 核心
          set_kv CONFIG_KSU y
          set_kv CONFIG_KSU_MANUAL_HOOK y  # manual hook 模式，不靠 kprobe
          set_kv CONFIG_OVERLAY_FS y       # KSU 硬依赖
          # KSU_MANUAL_HOOK 的 Kconfig 依赖 KPROBES (即使不用)
          set_kv CONFIG_KPROBES y
          set_kv CONFIG_HAVE_KPROBES y
          set_kv CONFIG_KPROBE_EVENTS y
          # kallsyms
          set_kv CONFIG_KALLSYMS y
          set_kv CONFIG_KALLSYMS_ALL y
          # 模块支持
          set_kv CONFIG_MODULES y
          set_kv CONFIG_MODULE_UNLOAD y
          # KPM 在 4.14 上风险未知，先关
          unset_kv CONFIG_KPM
          # SUS_SU default y, 非 GKI 下会导致 ksu_devpts_hook 未定义, 显式关
          unset_kv CONFIG_KSU_SUSFS_SUS_SU

          # SUSFS 全家桶
          set_kv CONFIG_KSU_SUSFS y
          set_kv CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT y
          set_kv CONFIG_KSU_SUSFS_SUS_PATH y
          set_kv CONFIG_KSU_SUSFS_SUS_MOUNT y
          set_kv CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT y
          set_kv CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT y
          set_kv CONFIG_KSU_SUSFS_SUS_KSTAT y
          set_kv CONFIG_KSU_SUSFS_SUS_OVERLAYFS y
          set_kv CONFIG_KSU_SUSFS_TRY_UMOUNT y
          set_kv CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT y
          set_kv CONFIG_KSU_SUSFS_SPOOF_UNAME y
          set_kv CONFIG_KSU_SUSFS_ENABLE_LOG y
          set_kv CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG y
          set_kv CONFIG_KSU_SUSFS_OPEN_REDIRECT y
          set_kv CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS y

          echo "--- 关键 config 状态 ---"
          grep -E "^(#\s*)?CONFIG_(KSU|KPROBES|KPM|OVERLAY_FS|KALLSYMS)" "$D" | sort -u

      - name: Build kernel
        working-directory: kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/toolchain/clang/bin:$GITHUB_WORKSPACE/toolchain/gcc-arm64/bin:$GITHUB_WORKSPACE/toolchain/gcc-arm/bin:$PATH"
          export KBUILD_BUILD_USER=github
          export KBUILD_BUILD_HOST=actions

          MAKE_ARGS=(
            O=out
            ARCH=arm64
            CC=clang
            LD=ld.lld
            AR=llvm-ar
            NM=llvm-nm
            OBJCOPY=llvm-objcopy
            OBJDUMP=llvm-objdump
            STRIP=llvm-strip
            CROSS_COMPILE=aarch64-linux-android-
            CROSS_COMPILE_ARM32=arm-linux-androideabi-
            CLANG_TRIPLE=aarch64-linux-gnu-
          )

          make "${MAKE_ARGS[@]}" "${DEFCONFIG}"

          echo "=== final .config sanity ==="
          grep -E "^CONFIG_(KSU|KSU_MANUAL_HOOK|KPM|OVERLAY_FS|KPROBES|KSU_SUSFS)=" out/.config | head -25

          set -o pipefail
          make -j"$(nproc)" "${MAKE_ARGS[@]}" 2>&1 | tee build.log

      - name: Locate build artifacts
        working-directory: kernel
        run: |
          ls -la out/arch/arm64/boot/ || true
          for f in Image.gz-dtb Image-dtb Image.gz Image; do
            if [ -f "out/arch/arm64/boot/$f" ]; then
              echo "FOUND: $f"
              cp "out/arch/arm64/boot/$f" "$GITHUB_WORKSPACE/$f"
            fi
          done

      - name: Upload kernel image
        uses: actions/upload-artifact@v4
        with:
          name: phoenix-sukisu-kernel
          path: |
            Image*
            kernel/build.log
          if-no-files-found: error

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: kernel/build.log
          if-no-files-found: ignore
